{
  "name": "typesafe-mcp",
  "version": "0.1.0",
  "description": "Local stdio MCP server exposing TypeSafe's System One API as MCP tools, for Claude Code / agent configs.",
  "type": "module",
  "bin": {
    "typesafe-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@typesafe-ai/sdk": "^0.6.0",
    "dotenv": "^18.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^5.0.1"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
